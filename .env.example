# MeTube Environment Configuration Example
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# AWS Configuration (for S3 uploads)
# Option 1: Use AWS SSO Profile (recommended for local development)
AWS_PROFILE=dev

# Option 2: Use direct credentials (alternative to AWS_PROFILE)
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key

# S3 Configuration (required for S3 upload feature)
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1
S3_PREFIX=metube-downloads

# Optional: For S3-compatible services (MinIO, DigitalOcean Spaces, etc.)
# S3_ENDPOINT_URL=http://localhost:9000

# Browser Cookies Configuration (required for YouTube as of 2025)
# Specify browsers to try in order (comma-separated)
# If first browser isn't available, it will try the next one
# Supported: chrome, firefox, brave, safari, edge, chromium, opera, vivaldi
COOKIES_FROM_BROWSER=brave,chrome,firefox,safari

# MeTube Configuration (optional, uncomment to override defaults)
# DOWNLOAD_DIR=/path/to/downloads
# MAX_CONCURRENT_DOWNLOADS=3
# LOGLEVEL=INFO

# Video Segmentation Configuration
# Duration of each segment in seconds (default: 30)
# Set to empty or 0 to disable segmentation
SEGMENT_DURATION=30
